# Everysearch ロードマップ

実装済み機能は [architecture.md](architecture.md) §10。  
**要望は「ユーザー追記欄」へ。**  
すぐサーチへ後で反映するためのメモもここに残す（すぐサーチ本体は 2026-09-02 より開発一時停止）。

最終更新: 2026-09-02

---

## 方針（2026-09-02）

| 項目 | 内容 |
|------|------|
| すぐサーチ | **開発一時停止**（指示があるまで実装しない） |
| Everysearch | **継続**。まず **どの Windows でも setup 可能な自己更新** を作り込む |
| 機能追加・バグ修正 | **本 roadmap に記載し続け**、将来すぐサーチへ反映できるようにする |
| 配布 | `%LocalAppData%\Everysearch\` 配置＋デスクトップショートカット。EXE 差し替え型更新 |

---

## 決まったこと（2026-07-27・第 1 段階で対応済み）

| 決定 | 対応 |
|------|------|
| ルートは README だけ。実行物は `script` | `script\build_exe2.bat` → `dist\Everysearch.exe` |
| D&D はいったん白紙、ノウハウ残す | development-guide §6 |
| パスワード DPAPI | architecture §8 |
| 名前列ソートはファイル名 | 修正済 |
| フォルダ変更で即再検索 | 修正済 |

---

## 優先: セットアップ＋自己更新（実装中）

| # | 項目 | 状態 | 内容 |
|---|------|------|------|
| U1 | setup（どの Windows でも） | **済** | `script\setup.bat` → LocalAppData＋デスクトップ／スタート。Python 不要 |
| U2 | 版管理 | **済** | `version.txt` / タイトルバー表示 |
| U3 | 「更新」タブ | **済** | 更新確認 → ダウンロードして更新／あとで。**GitHub 文言・トークン UI なし** |
| U4 | 差し替え | **済** | staging → previous → current。同じショートカット |
| U5 | 失敗時 | **済（土台）** | `previous\` に直前版を残す |
| U6 | settings 隔離 | **済** | `%LocalAppData%\Everysearch\data\settings.json` |
| U7 | 配布元を公開 | **済** | Release 公開。利用者はトークン不要（裏で Releases を参照） |
| U8 | 個人パス・開発用語を UI に出さない | **済** | 利用者向け文言のみ |

配置イメージ:

```text
%LocalAppData%\Everysearch\
  current\Everysearch.exe
  current\version.txt
  previous\          … 直前版
  staging\           … DL 中
  data\settings.json
  update_runner.bat  … 差し替え用（本体終了後に実行）
```

GitHub: `https://github.com/rittyapp/Everysearch` の **Releases** に `Everysearch.exe` または zip を添付する運用。

---

## 第 2 段階（機能追加・後回し可／すぐサーチ反映候補）

| # | 項目 | すぐサーチ反映 | 実装方針 |
|---|------|----------------|----------|
| 1 | 更新日時での絞り込み | 候補 | 期間チップ |
| 2 | 表示件数の上限変更 | 候補 | フィルタ設定に欄 |
| 3 | 検索キーワード履歴 | 候補 | ↓／空欄フォーカスで履歴 |
| 4 | 結果一覧エクスポート | 候補 | CSV / タブ区切り |
| 5 | 対象フォルダ複数指定 | 候補 | `;` 区切り OR |
| 6 | フィルタプリセット | 候補 | 名前付き定義一式 |
| 7 | D&D 作り直し | 低 | UI スレッドを止めない前提 |

---

## 見つかった不具合・要対応

| # | 項目 | 状態 | 内容 | すぐサーチ反映 |
|---|------|------|------|----------------|
| A | フィルタ設定レイアウト崩れ | **修正済** | grid＋スクロール | UI 作るとき同趣旨 |
| B | 狭いウィンドウで潰れ | 要確認 | 最低幅など | 同上 |
| C | 色プレビュー追従 | 未着手 | 入力中プレビュー | 同上 |
| D | フィルタ全OFFで0件 | 仕様確認 | 全OFF＝全表示にするか | 種類チップと同論 |
| E | EXE 再ビルド | 運用注意 | setup／Release 前に build_exe2 | — |
| F | フィルタ全ON/OFF トグル | 済 | 「フィルタ」ラベルで切替 | 済（すぐサーチ種類） |

---

## すぐサーチへの反映メモ（停止中でも追記する）

Everysearch で入れた改善は、再開後にすぐサーチへ移植しやすいようここに要約を足す。

| 日付 | Everysearch の変更 | すぐサーチへの示唆 |
|------|-------------------|-------------------|
| 2026-09-02 | フィルタ設定 UI grid 化 | 設定画面は pack 横並びを避ける |
| 2026-09-02 | 自己更新（setup＋Releases） | Phase 5 は Everysearch 型をコピー |
| 2026-09-02 | フィルタ全ON⇔全OFF | 種類ラベルと同様（済） |

---

## ユーザー追記欄

（自由記述）
